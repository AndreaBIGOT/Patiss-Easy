{% extends 'base.html.twig' %}

{% block title %}Recettes EASY{% endblock %}

{% block body %}
    {# {{ form_start(form)}}
    {{ form_row(form.username, {'label': "Nom d'utilisateur",'attr': {'placeholder': 'Entrez votre pseudo'}})}}
    {{ form_row(form.email, {'label': "Adresse mail",'attr': {'placeholder': 'Entrez votre adresse mail'}})}}
    {{ form_row(form.password, {'label': "Mot de passe",'attr': {'placeholder': 'Entrez votre mdp'}})}}

    <button type="submit" class="btn btn-success">Inscription</button>
    {{ form_end(form)}} #}

    <form action="{{ path('securityLogin')}} " method="POST"> 
        <div class="form-group">
            <input placeholder="mail"  required name="_username" type="text" class="form-control">
        </div>
        <div class="form-group">
            <input placeholder="mdp"  required name="_password" type="password" class="form-control">
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-success">Connexion</button>
        </div>
    </form>
{% endblock %}
