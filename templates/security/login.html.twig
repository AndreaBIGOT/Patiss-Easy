{% extends 'base.html.twig' %}

{% block title %}Recettes EASY{% endblock %}

{% block body %}

    <form action="{{ path('securityLogin')}} " method="POST"> 
        <div class="form-group">
            <input placeholder="mail"  required name="_username" type="text" class="form-control">
        </div>
        <div class="form-group">
            <input placeholder="mdp"  required name="_password" type="password" class="form-control">
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-success">Connexion</button>
        </div>
    </form>
{% endblock %}
